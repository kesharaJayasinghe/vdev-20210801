import 'package:flutter/material.dart';

class VerticalSpacing extends SizedBox {
  final double height;
  final double width;
  VerticalSpacing({this.height, this.width})
      : super(height: height, width: width);
}
